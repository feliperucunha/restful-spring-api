# Backend made using Spring Boot and MySQL
 - It has Docker capability using the commands:
    - mvn clean install (compile)
    - docker-compose up --build --force-recreate (run)
   
 - When running for the second time and on:
    - docker-compose up